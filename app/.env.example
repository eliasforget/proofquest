# Optional for v0.4 public GitHub repository analysis.
# Without a token, GitHub allows unauthenticated public API requests with a lower rate limit.
# Never expose this token through NEXT_PUBLIC_* variables.
GITHUB_TOKEN=
